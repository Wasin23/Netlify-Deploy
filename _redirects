# Email tracking redirects for ExaMark
# These redirects handle email opens and clicks properly

# Pixel tracking - redirect to function
/track/pixel/:id.png /.netlify/functions/track-pixel?email_id=:id 200

# Click tracking - redirect to function  
/track/click/:id /.netlify/functions/track-click?email_id=:id 200

# Mailgun webhook for email replies
/webhooks/mailgun /.netlify/functions/mailgun-webhook 200

# Fallback pixel tracking (without .png extension)
/track/pixel/:id /.netlify/functions/track-pixel?email_id=:id 200

# Test redirects
/test-redirect /pixel.html 200
